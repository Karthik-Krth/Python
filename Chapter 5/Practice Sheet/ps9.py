s = {8, 7, 12, "Harry", [1,2]} #cannot change values in sets
 # invalid statement as list are mutable it can't be placed in sets , we use tuple instead of lists

 # i.e s = {8, 7, 12, "Harry", (1,2)}
